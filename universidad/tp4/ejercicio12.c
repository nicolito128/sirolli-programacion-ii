#include <stdio.h>
#define listSize 100

// DISTANCIA: raiz2(x^2 + y^2)

typedef float list[listSize];

void getPointsXY(list, list, int*);
void searchHigherDistanceByQuadrant(int, list, list, int*);
void showPointsByDistance(list, list, int*, float);
void searchPoint(list, list, int*, float, float); // retorna la posicion

int main() {
	
	return 0;
}

